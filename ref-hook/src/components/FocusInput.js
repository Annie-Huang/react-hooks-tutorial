import React, {useEffect} from 'react';

const FocusInput = () => {

    useEffect(() => {
        // focus the input element
    }, []);

    return (
        <div>
            <input type="text"/>
        </div>
    );
};

export default FocusInput;
